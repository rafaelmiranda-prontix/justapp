apiVersion: batch/v1
kind: Job
metadata:
  name: justapp-migrate
  namespace: justapp
spec:
  template:
    spec:
      initContainers:
      - name: wait-for-postgres
        image: busybox:1.35
        command: ['sh', '-c']
        args:
        - |
          until nc -z postgres-service 5432; do
            echo "Waiting for PostgreSQL..."
            sleep 2
          done
          echo "PostgreSQL is ready!"
      containers:
      - name: migrate
        image: legalconnect-app:latest  # Mesma imagem do app
        command:
        - sh
        - -c
        - |
          echo "Running Prisma migrations..."
          npx prisma generate
          npx prisma migrate deploy
          echo "Migrations completed!"
        envFrom:
        - configMapRef:
            name: justapp-config
        - secretRef:
            name: justapp-secrets
        env:
        - name: DATABASE_URL
          value: "postgresql://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@postgres-service:5432/$(POSTGRES_DB)?schema=public"
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: justapp-config
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: justapp-secrets
              key: POSTGRES_PASSWORD
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: justapp-config
              key: POSTGRES_DB
      restartPolicy: Never
  backoffLimit: 3
